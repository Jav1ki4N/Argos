IMPORTANT: IF U ARE TO MODIFY ANY FILE, ASK ME FOR PERMISSION AT FIRST.

1. For every newly done function, add comment in it's header, e.g:
```c
//  func     XXX
/// @brief   XXX
/// @param f XXX
void foo(int f)
```

2. when use (condition)?a:b, if a or b is too long, write separately, e.g.
```c
c = (condition)a:
               b
```

3. use some_var for variable; doSomething for function, SOME_CONST for const and SomeClass for class / enum / type / struct

